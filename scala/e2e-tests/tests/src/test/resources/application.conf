input-bucket = "intg-dr2-ingest-raw-cache"
input-bucket = ${?INPUT_BUCKET}
lock-table-name = "intg-dr2-ingest-lock"
lock-table-name = ${?LOCK_TABLE_NAME}
ingest-sqs-queue = ${INGEST_SQS_QUEUE}
notifications-sqs-queue = ${NOTIFICATIONS_SQS_QUEUE}
preingest-sfn-arn = ${PREINGEST_SFN_ARN}
copy-files-dlq = ${COPY_FILES_DLQ}
