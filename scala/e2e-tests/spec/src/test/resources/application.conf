input-bucket = "input-bucket"
lock-table-name = "lock-table"
ingest-sqs-queue = "ingest-queue"
notifications-sqs-queue = "notifications-queue"
preingest-sfn-arn = "preingest-arn"
copy-files-dlq = "copy-files-dlq"
