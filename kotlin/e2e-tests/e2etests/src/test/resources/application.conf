s3Bucket = ${S3_BUCKET}
adhocBucket = ${ADHOC_BUCKET}
driBucket = ${DRI_BUCKET}
sqsQueue = ${INGEST_SQS_QUEUE}
judgmentSqsQueue = ${JUDGMENT_SQS_QUEUE}
adhocSqsQueue = ${ADHOC_SQS_QUEUE}
driSqsQueue = ${DRI_SQS_QUEUE}
copyFilesLogGroup = ${COPY_FILES_LOG_GROUP}
copyAdhocFilesLogGroup = ${COPY_ADHOC_FILES_LOG_GROUP}
copyDRIFilesLogGroup = ${COPY_DRI_FILES_LOG_GROUP}
lockTable = ${LOCK_TABLE}
externalLogGroup = ${EXTERNAL_LOG_GROUP}
preingestSfnArn = ${PREINGEST_SFN_ARN}
