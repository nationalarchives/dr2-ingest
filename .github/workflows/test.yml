name: DR2 Run Lambda Tests
on:
  push:
    branches-ignore:
      - main
      - release-*
permissions:
  id-token: write
  contents: read
jobs:
  test:
    steps:
      - name: check SSL version
        run: openssl version
